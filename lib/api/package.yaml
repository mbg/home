_common: !include "../../common/hpack/lib.yaml"

name: home-api
<<: *common

library:
  source-dirs: src

  dependencies:
    - aeson
    - deriving-aeson
    - home-core
    - home-db
    - monad-logger
    - servant-server
    - warp
    - yaml
